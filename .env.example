# ============================================================
# GM-back — exemple de configuration d'environnement
# Copie ce fichier en `.env` et remplis les valeurs
# ============================================================

# -- Serveur --
NODE_ENV=development
PORT=3000

# -- Base de données (MySQL distant) --
DB_HOST=10.173.88.12
DB_PORT=3306
DB_NAME=gm_hackaton
DB_USER=
DB_PASSWORD=

# -- Auth (JWT) --
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=7d
